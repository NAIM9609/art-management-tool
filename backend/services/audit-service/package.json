{
  "name": "audit-service",
  "version": "1.0.0",
  "private": true,
  "description": "AWS Lambda handlers for Audit Service API",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch"
  }
}
