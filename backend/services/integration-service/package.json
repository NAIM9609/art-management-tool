{
  "name": "integration-service",
  "version": "1.0.0",
  "private": true,
  "description": "AWS Lambda handlers for Integration Service API (Etsy)",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch"
  }
}
